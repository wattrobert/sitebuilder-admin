import React from "react";
import Typography from "@material-ui/core/Typography";

class ServerManager extends React.Component {
  public render() {
    return (
      <React.Fragment>
        <Typography variant="headline">Server Manager</Typography>
      </React.Fragment>
    );
  }
}

export default ServerManager;
